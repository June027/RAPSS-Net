# RAPSS-Net dependencies
#
# --------------------------------------------------------------------------------
# Install a CUDA-compatible PyTorch build for your platform before optional optimized Mamba extensions.
# The repository includes a pure-PyTorch reference Mamba implementation.
# This file lists the remaining dependencies that have been validated on a system
# with CUDA 12.1, PyTorch 2.1.0, and an NVIDIA RTX 4090.
# --------------------------------------------------------------------------------

# --- Core Frameworks & Data Science ---
torch==2.1.0
torchvision==0.16.0
torchaudio==2.1.0
# mamba-ssm can optionally replace the included reference implementation on Linux/CUDA
# causal-conv1d is only needed by optimized mamba-ssm builds
triton==2.1.0; platform_system != "Windows"
numpy==1.26.4
PyYAML==6.0.3
pandas==2.3.3
scikit-learn==1.2.2
scipy==1.11.0
monai==1.3.0

# --- Data Processing & Augmentation ---
scikit-image==0.20.0
nibabel==5.3.3
SimpleITK==2.5.3  # Corrected from simpleitk
openpyxl==3.1.5
einops==0.8.2
tqdm==4.65.0

# --- Visualization & Logging ---
matplotlib==3.10.7
seaborn==0.12.2 # Using a common compatible version
tensorboard==2.12.3

# --- Other Utilities ---
# These packages are often dependencies of the above libraries.
# Pinning them ensures maximum reproducibility.
absl-py==2.4.0
attrs==25.4.0
certifi==2022.12.7
charset-normalizer==2.1.1
cycler==0.12.1
fonttools==4.61.1
idna==3.4
joblib==1.5.3
kiwisolver==1.4.9
MarkupSafe==2.1.5
packaging==25.0
Pillow==12.0.0
python-dateutil==2.9.0.post0
pytz==2025.2
requests==2.28.1
six==1.17.0
threadpoolctl==3.6.0
urllib3==1.26.13
